set(DEMO_DEST examples/graphics)
add_demo(demo_vulkan LINK xci-graphics DEPS share)
if (XCI_TEXT)
    add_demo(demo_coords LINK xci-text xci-graphics DEPS share)
    add_demo(demo_shapes LINK xci-text xci-graphics DEPS share)
endif()
if (XCI_WIDGETS)
    add_demo(demo_fps LINK xci-widgets xci-text xci-graphics DEPS share)
endif()
